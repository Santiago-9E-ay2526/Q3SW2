// Function for Discount Button
function discountButton (){
     let firstBudget = document.getElementById(laptopbudget).value;
     let discountPrice = Number(firstBudget * 0.2);

     document.getElementById().innerHTML =;
}